<li id="car-<%= car.id %>">
  <span class="carname" style="font-weight:bold"><%= car.carname %></span><br>
  <span class="description"><%= car.description %></span><br>
  Score: 
  <% if car.score? %>
    <%= car.score %>
  <% else %>
    empty
  <% end %>
  <br>
</li>
