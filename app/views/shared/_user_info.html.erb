<%= link_to gravatar_for(current_user, size: 50), current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to "查看个人资料", current_user %></span>
<span><%= pluralize(current_user.comments.count, "车评") %></span>
